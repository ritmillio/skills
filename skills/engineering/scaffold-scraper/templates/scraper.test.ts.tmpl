import { describe, it, expect } from "vitest";
import { __VAR__R2Keys } from "./r2-keys.js";

describe("__COUNTRY__ __VERTICAL_TITLE__ r2-keys", () => {
  it("keys under the __R2_PREFIX__ prefix", () => {
    // TODO(scaffold): once the doc type is real, build a fixture and assert.
    expect(__VAR__R2Keys.getPrefix()).toBe("__R2_PREFIX__/");
    expect(__VAR__R2Keys.getPrefix(2015)).toBe("__R2_PREFIX__/2015/");
  });
});

// TODO(scaffold): port the reference vertical's scraper.test.ts — parsing
// fixtures, gap handling, and (in scraper.live.test.ts) a real network probe.
